import React from 'react';
import { motion } from 'framer-motion';

const SkillsBlock = () => {
    return (
        <motion.div className="snap-center w-full min-h-screen select-none flex flex-col items-center justify-between  bg-gradient-to-bl from-[#ae8b9c] via-[#8baaaa] to-[#8baaaa]"></motion.div>
    );
};

export default SkillsBlock;
